import React from 'react'

const BaseLine = ({className = ''}) => {
  return (
    <div className={`bg-primary p-[1px] w-40 rounded-xl ${className} mx-auto`}>
    </div>
  )
}

export default BaseLine
