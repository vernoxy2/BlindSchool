import React from 'react'

const BaseLine = ({className = ''}) => {
  return (
    <div data-aos="zoom-in" className={`bg-primary p-[1px] w-40 rounded-xl ${className} `}>
    </div>
  )
}

export default BaseLine
